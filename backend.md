Follow Routes (API calls) -> Controller (Handle business logic) -> Services (Connect to database) -> Repositories (Database logic) -> Models (Schema) -> Utilities (Helper functions) 
Architecture 
Should have clean code

USE typecript +